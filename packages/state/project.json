{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@pomofocus/state",
  "sourceRoot": "packages/state/src",
  "projectType": "library",
  "tags": ["type:state", "scope:shared"],
  "targets": {
    "type-check": {
      "command": "tsc -p tsconfig.lib.json --noEmit",
      "options": {
        "cwd": "packages/state"
      }
    }
  }
}
